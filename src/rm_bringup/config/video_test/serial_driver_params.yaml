/**:
  ros__parameters:
    target_frame: odom
    timestamp_offset: 0.006
    reconnect_interval_ms: 500
    port_name: "/dev/ttyACM0"
    protocol: "infantry_32"
    enable_data_print: false # 改为true将会打印从串口读出来的16进制数据


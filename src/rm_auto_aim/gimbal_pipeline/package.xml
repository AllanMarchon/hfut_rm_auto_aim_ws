<?xml version="1.0"?>
<?xml-model
   href="http://download.ros.org/schema/package_format3.xsd"
   schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>gimbal_pipeline</name>
  <version>1.0.0</version>
  <description>Unified gimbal pipeline: merges max_entropy_tracker + target_selector + gimbal_controller into a single node to eliminate inter-node ROS2 communication latency.</description>
  <maintainer email="your_email@example.com">Maintainer Name</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Common dependencies -->
  <depend>rclcpp</depend>
  <depend>rclcpp_components</depend>
  <depend>rm_interfaces</depend>
  <depend>rm_utils</depend>

  <!-- Message types -->
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>visualization_msgs</depend>
  <depend>std_msgs</depend>

  <!-- TF2 -->
  <depend>tf2</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>

  <!-- Math -->
  <depend>angles</depend>
  <depend>eigen</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>
  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>

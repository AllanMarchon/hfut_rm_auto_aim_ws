#include "models/model_factory.h"
#include "models/model_config_loader.h"
#include <iostream>
#include <memory>

int main() {
    std::cout << "Factory demo moved to examples directory.\n";
    std::cout << "To build this demo, either move it into a package that depends on basic_models and rclcpp,\n";
    std::cout << "or add appropriate find_package() and link directives in CMakeLists.txt.\n";
    return 0;
}
